# igugyj.github.io

Pure static redirect service via GitHub Pages.

## Usage

Edit `redirects.json` and push to `main`. GitHub Action auto-generates HTML and deploys to `gh-pages`.

## Local Test

```bash
py generate.py
```
